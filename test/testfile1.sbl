version 1;

shader "Empty Shader" {};

shader "Uniform Phong Shader"
{
    shared = <<<
        /* this piece of code will be pasted in every shader */
    >>>;

    vertex(vec3 position, vec3 normal): vec3 transformedNormal = <<<
        /* vertex shader main function */
    >>>;

    fragment(): (vec3 a, vec3 b) = <<<
        /* fragment shader main function */
    >>>;
};
